{
  "schema_version": 1,
  "updated_at": "2026-08-02T12:42:28+08:00",
  "repository": {
    "url": "https://github.com/Aladdin-Wang/Mklink-AI-Probe",
    "project_root": "Mklink-AI-Probe",
    "branch": "fix/hpm5301-online-symbols",
    "head": "Branch fix/hpm5301-online-symbols contains Serial Assistant implementation commit 4e5ff35; the current handoff commit records that branch checkpoint.",
    "remote_head": "The authorized GitHub branch push carries implementation commit 4e5ff35 and its handoff record to origin/fix/hpm5301-online-symbols. GitHub and Gitee master remain on the published v0.1.4 baseline.",
    "working_tree": "Serial Assistant source, tests, production assets, and project memory are committed. Only excluded local .mklink state and Python caches remain untracked. No merge, additional push, tag, or release is authorized."
  },
  "mission": {
    "product": "MKLink/MicroLink tooling for firmware flash, embedded debug, symbols, memory, RTT/SystemView/VOFA/SuperWatch streaming, Serial, Modbus, MCP, Web, and Tauri desktop workflows.",
    "maintenance": "Keep this file limited to the current baseline, durable decisions, latest qualification evidence, unresolved limits, and next actions. Git history is the record for completed chronology."
  },
  "current_session": {
    "status": "serial_assistant_automated_and_web_ready_awaiting_uart_hardware",
    "current_task": "Serial Monitor is renamed Serial Assistant and now has confirmed 日志 | 终端 modes. Immediate Base64 raw chunks feed the xterm terminal without waiting for LF while structured RX/TX line events remain parallel for Log mode. Serial Assistant no longer depends on MKLink deviceConnected, preserves the backend session across component unmounts, restores a running port from status, keeps serial transmit history separate from RTT, and only renders RX in the terminal so target echo remains authoritative. Automated and disconnected-device Web checks are complete; real UART RX/TX, ANSI, keyboard, reconnect, same-port conflict, and simultaneous RTT validation must run after the maintainer connects the serial fixture.",
    "release": "Official v0.1.4 remains the published baseline. Serial Assistant implementation checkpoint 4e5ff35 is pushed only to the GitHub fix branch and remains unreleased pending real-UART qualification."
  },
  "milestones": [
    {
      "name": "Product and distribution baseline",
      "status": "complete",
      "notes": "Online/offline flash, debug control, symbols and types, bounded binary streaming, Serial, Modbus, MCP, Web, Tauri v2, bundled Python sidecar, USB Web entry, browser file uploads, updater flow, and resource arbitration are implemented."
    },
    {
      "name": "Flash, symbols, and RTT correctness",
      "status": "complete",
      "notes": "Builtin pyelftools is authoritative by default; aggregate symbols and bounded pasted C layouts work; symbol reload is a shared device transaction; HPM low-address writable ELF sections can supply _SEGGER_RTT; successful automatic connections backfill the actual serial port; online/offline flash workflows retain files and rebuilt-image changes correctly."
    },
    {
      "name": "HPM5301 ROM API and SuperWatch",
      "status": "complete",
      "notes": "HPM targets use the device ROM API without FLM. SuperWatch uses a fixed-duration oscilloscope timeline that follows live data with an adjustable fixed lag, freezes on pause, and supports retained-history navigation. HPM online flash synchronously stops SuperWatch, closes the shared Device, and releases the probe before programming."
    },
    {
      "name": "Global GUI localization and navigation",
      "status": "complete",
      "notes": "One persistent Chinese/English switch controls the Web/Tauri shell and embedded waveform workspace. The shell brand is MKLink. Technical English names remain unchanged. Dashboard order is RTT View, SuperWatch, HardFault, Memory, Debug Control, Serial Assistant, Modbus, RTOS Trace, and Symbols last. The content card starts directly with a compact tab row; the redundant Dashboard heading is removed and the resource-owner status shares that row. RTT View keeps address/source on one row and grouped acquisition, encoding, metrics, and secondary actions on the next; data-format examples open on demand instead of consuming a permanent third row."
    },
    {
      "name": "Lazy large-array symbol browsing",
      "status": "complete",
      "notes": "Arrays are grouped into 256-direct-element range nodes and expanded on demand. Structure arrays and nested arrays retain their hierarchy; exact backend search resolves unloaded paths; later elements support type lookup and the normal SuperWatch/read/write descriptor path. The old first-256-leaves warning is removed."
    },
    {
      "name": "HPM5301 SystemView recorder and analysis",
      "status": "complete",
      "notes": "RTOS Trace shares RTT address state with RTT View and uses the probe SystemView bridge plus SEGGER UART Recorder handshake. Delayed TaskList recovery, parser framing recovery, valid HPM ram_base=0/id_shift=0, exact Runtime/Context analysis, inactive task retention, and running-session metadata hydration restore task1/task2/task3/Tmr Svc correctly. Timeline uses a microsecond ruler, 10-second default, 10/30/60-second choices, 300,000-event browser ring, wheel zoom, drag pan, and Fit All. Explicit JSONL recording is independent of display and acquisition; offline replay is bounded and supports pause, speed, progress, and restart."
    },
    {
      "name": "RTT terminal mode",
      "status": "complete",
      "notes": "RTT remains channel 0 only. Binary terminal chunks decode incrementally without waiting for LF, while existing line and waveform parsing remain parallel. The Web view switches between 日志 and 终端 without restarting RTT; xterm.js handles common ANSI colors, cursor movement, erase, and clear controls, with SEGGER 2;3Xm and 4;4Xm compatibility normalization. xterm convertEol is enabled so RT-Thread bare LF and conventional CRLF both begin the next display row at column zero without rewriting backend data. Keyboard input is batched and serialized through the existing RTT Down Buffer write API; termios, PTY/ConPTY, multi-channel UI, and SEGGER virtual terminals are intentionally out of scope."
    },
    {
      "name": "Serial Assistant terminal and independent lifecycle",
      "status": "awaiting_hardware",
      "notes": "Serial Assistant provides 日志 and xterm terminal modes without requiring an MKLink target connection. The backend publishes immediate exact RX/TX chunks alongside existing structured events; the terminal consumes RX only, uses shared ANSI/SEGGER normalization and convertEol, and forwards serialized keyboard bytes only while the selected UART is open. Explicit Open/Close controls own the backend lifecycle; mode, dashboard-tab, and route changes close only the browser subscription. Serial and RTT keep separate resources, settings, histories, status, and transports. Automated and disconnected-device Web validation passed; real UART qualification remains required."
    }
  ],
  "verification": [
    {
      "area": "Current automated gate",
      "result": "Python passed 1037 tests with 1 skipped; GUI passed 44 files and 477 tests; the Vite 8.1.5 production build and git diff --check passed. Focused Serial Assistant coverage passed 3 Python tests and 27 GUI tests together with the shared RTT terminal/transmit and dashboard navigation coverage."
    },
    {
      "area": "Large-array symbol browsing",
      "result": "On the real STM32 AXF, _thread_stack exposed eight 256-element ranges through [1792..2047]. The tail request returned exactly _thread_stack[1792] through _thread_stack[2047], exact search found unloaded _thread_stack[2047], type lookup reported uint8_t at 0x20009A63, and target read returned 35. No write was performed because the available large arrays are active stack/buffer memory; the dynamic write path is covered automatically. SuperWatch and Symbols Web views both rendered only the selected tail page, omitted [0], removed the old warning, and produced no console warnings or errors."
    },
    {
      "area": "HPM5301 flash and dump-memory",
      "result": "The hello_world sample rebuilt, programmed, and verified through the HPM ROM API at 0x80000400. One 4-byte region sustained 6,935.9 Hz and three regions totaling 9 bytes sustained 2,416.4 Hz in 30-second runs with no flags, parser warnings, or failures. SuperWatch also accumulated more than 100,000 complete samples without host drops and released the probe before online flash."
    },
    {
      "area": "STM32F103RC dump-memory throughput",
      "result": "Keil ARMClang rebuilt and downloaded the STM32F103RC target with no errors or warnings. Separate-region dump-memory sweeps reached about 30.22/16.53/8.87/4.50/2.29 kHz for 1/2/4/8/16 regions; the 16-region 30-second run sustained 2,262.44 Hz with zero flags or mismatches. SuperWatch coalesced 16 contiguous floats into one 64-byte block and rendered all channels at 6,636.66 Hz over 30 seconds with no acquisition, parser, backend, or transport drops; the Hz-only badge matched an independent counter measurement."
    },
    {
      "area": "SuperWatch Web interaction baseline",
      "result": "Real STM32 Web tests qualified the complete current interaction model: configurable history up to 1,000,000 points/channel without changing the live span, frozen pause history with pan/zoom/minimap browsing, live X zoom and navigation that preserve a fixed lag while advancing, double-click reset to the newest window, independent split-channel Y axes with shared X, persistent merge control, and an auto-hiding channel legend. The one-request-in-flight Worker envelope scheduler remains responsive near 9.26 kHz, and the tested surfaces produced no transport/backend drops or browser warnings/errors."
    },
    {
      "area": "Symbols, RTT, and GUI hardware loop",
      "result": "Builtin ELF resolved HPM low-address _SEGGER_RTT and portable writable variables. RTT auto-search and address sharing worked. The source Web GUI was exercised in Chinese and English at desktop and mobile sizes without relevant console errors or document overflow."
    },
    {
      "area": "SystemView hardware loop",
      "result": "The HPM target streamed at 360 MHz with four correctly named tasks, late-attach metadata recovery, 10/30/60-second views, a 300,000-event browser ring, and zero parser packet drops. Independent JSONL recording captured about 42,844 events over 21 seconds while Trace continued; bounded replay supports pause, speed, progress, and restart."
    },
    {
      "area": "Current live surface",
      "result": "The source Web GUI is available at http://127.0.0.1:8765 with the STM32 project connected and its AXF loaded. It is left on the updated RTT View in terminal mode. Desktop and 768px-wide browser checks confirmed the MKLink brand, two-row RTT controls, contained format-help popover, preserved resource status, no document-width overflow, and no console warnings or errors."
    },
    {
      "area": "RTT terminal Web and STM32 hardware loop",
      "result": "The STM32F103RC test firmware rebuilt with Keil ARMClang without errors or warnings and was downloaded with IDE verify OK. Triggering its controlled ANSI self-test rendered RT-Thread green/yellow/red/magenta log colors, SEGGER normal and bright foreground/background macros, ESC[2K line erase, cursor overwrite, ESC[2J clear/home, and a split CSI sequence in the xterm terminal. Terminal keyboard input reached RTT channel 0 and returned 0d (Enter), 7f (Backspace), and 1b 5b 41 (ArrowUp) through the diagnostic loopback. With the later Agile Console RTT firmware, the real MSH help command emitted bare-LF lines that all rendered from column zero; the prompt and consecutive rows had no staircase or extra blank lines, while Log mode retained all 37 raw rows. Log/terminal switching and view-local clear worked without stopping RTT; transport/backend drops stayed at 0. At a 768px viewport document width equaled the viewport and the terminal/control rows remained contained; browser warning/error logs were empty."
    },
    {
      "area": "Serial Assistant automated and disconnected-device Web loop",
      "result": "A synthetic partial prompt without LF reached the terminal chunk callback immediately while the structured log remained line-based. Base64 preserved ANSI and non-ASCII bytes exactly; RX/TX byte counters counted chunks once. Vue coverage verified no MKLink gate, session recovery from backend status, no local TX echo, exact keyboard HEX transmission, visible-mode-only clear, separate serial send history, and no backend stop on unmount. The real Web page rendered Serial Assistant in both modes with MKLink disconnected. Desktop and 768px checks had no document overflow, no control outside the Serial Assistant container, and no page warning/error logs. No physical UART was opened for this qualification."
    }
  ],
  "decisions": [
    "AGENTS.md and the repository maintenance Skill are authoritative. Before changing a product, interaction, architecture, workflow, or testing strategy, present the problem, options, tradeoffs, recommendation, and acceptance criteria, then wait for explicit maintainer confirmation. Confirmed-strategy root-cause fixes may proceed directly. Runtime changes require a fix/feature branch, full Python and GUI suites, production build, git diff check, project-memory update, and affected real-hardware loop. Never infer authority to commit, merge, push, sign, tag, publish, modify latest.json, or synchronize Gitee.",
    "HPM targets always use the dedicated ROM API and never FLM. Agent-driven download routing is IDE-native build/download, then pyOCD online flash, then MKLink offline deployment; do not silently switch after a started operation fails.",
    "Builtin pyelftools is the default ELF/DWARF backend. External GNU tools run only when explicitly selected. _SEGGER_RTT in ELF/AXF is accepted only when its complete storage lies in an allocated writable section.",
    "Symbol reload is one shared backend transaction bound to the current Device. Requested and active paths must match, and SuperWatch must be stopped/rebound/restarted serially. Pasted C layouts remain bounded and fingerprint-scoped.",
    "Large arrays page by 256 direct elements, represented as explicit [start..end] range nodes that load only when expanded. Paging is recursive for structure and nested arrays. Exact search resolves unloaded paths through the backend, and dynamically resolved descriptors remain valid for SuperWatch, type lookup, and read/write. Do not restore a first-256-readable-leaves limit or its UI warning.",
    "Dashboard sessions and one-shot debug/flash operations use serialized resource leases. User operations may preempt Dashboard owners only after synchronous stop succeeds; one user operation never preempts another. HPM online flash also closes the shared CDC Device before opening its ROM-API backend.",
    "Desktop and Web-entry clients stop only backend processes they created and still own. Web app shells use no-store, hashed assets may be immutable, and copied user-level Skill/runtime installations must not depend on the development checkout.",
    "SuperWatch uses a fixed-duration timeline independent of history capacity, defaults to zoom 2 at the newest window, and supports up to 1,000,000 points/channel with a 48-byte-per-point memory estimate and a warning above 512 MiB. Live horizontal interactions preserve the chosen time lag and span while advancing; pause freezes the complete frontend history and viewport; resume clears the snapshot; double-click restores the newest default window. VOFA retains its adaptive timeline.",
    "SuperWatch effective rate counts complete sample rows rather than variables and never uses the requested interval as the result. The backend uses a one-second perf_counter window as the authoritative status value; the binary viewer uses producer batch timestamps over at least 0.5 seconds for responsive updates between status polls and resets the statistic at stream/layout boundaries.",
    "SuperWatch defaults to one shared plot. One visible channel may move to a lower plot with an independent persisted Y range while both plots share X navigation. The auto-hiding legend uses a compact reveal button, and a persistent split divider names the independent channel and provides Merge.",
    "The global GUI language defaults to Chinese and persists under mklink_lang. English technical product names are not translated. Post-flash reconnection was explicitly canceled and must not be inferred.",
    "RTT View is intentionally fixed to channel 0 with a compact 日志 | 终端 mode switch. Terminal mode uses xterm.js with convertEol enabled so bare LF and CRLF are both display newlines, common ANSI parsing plus SEGGER macro normalization, retained history while hidden, and keyboard forwarding through the existing Down Buffer API. Backend and Log-mode bytes remain unchanged. Do not add termios, PTY/ConPTY, multi-channel UI, or SEGGER virtual-terminal support without a new confirmed strategy.",
    "Serial Assistant is independent of MKLink deviceConnected and uses the SERIAL_PORT resource while RTT uses MKLINK_BRIDGE/TARGET_DEBUG. Immediate raw serial chunks feed the terminal without waiting for LF; structured events remain parallel for Log mode. The terminal displays RX only and forwards raw keyboard bytes without local echo. Mode/tab/route changes preserve the backend session and close only the browser subscription; explicit Close Port is authoritative. Serial transmit settings/history remain separate from RTT. Physical UART and Modbus access to one COM port must still fail clearly rather than run concurrently.",
    "RTT View and RTOS Trace share one valid RTT address. SystemView uses SystemView.start plus the SEGGER recorder handshake and matching stop sequence on both ARM/SWD and HPM RISC-V/JTAG. START supplies INIT/SYSDESC/SYSTIME, delayed TaskList recovers startup metadata, ram_base=0 and id_shift=0 are valid, and late attach hydrates task, ISR, clock, drop, and recording metadata from backend status.",
    "SystemView uses complete bounded intervals for exact Runtime/Context analysis, retains inactive contexts, keeps 60 seconds of exact history and a 300,000-event browser ring, and offers 10/30/60-second views. JSONL recording is independent of display/acquisition, and offline replay is incremental and bounded with pause, speed, progress, and restart.",
    "MCP stdio reserves stdout for JSON-RPC. Ordinary Python output is redirected to stderr only for the MCP entry; CLI, Web, and desktop logging remain unchanged."
  ],
  "hardware": {
    "probe": "MKLink V4 is available; identifiers are intentionally omitted.",
    "target": "STM32F103RC and HPM5301 EVK Lite fixtures are available; local paths are intentionally omitted.",
    "permission": "Firmware build/flash and read-only target validation are permitted when required by the active task."
  },
  "known_limits": [
    "High-event-rate SystemView can overflow the target RTT buffer. Host TaskList retry recovers metadata, but physically dropped event packets cannot be reconstructed; increase the target buffer for loss-sensitive timing analysis.",
    "The first real V4 offline trigger once returned a transient empty failure and an immediate retry succeeded. Reproduce cold starts and add device-output diagnostics if it recurs.",
    "Full npm audit reports six high-severity development-only findings through the test dependency chain; runtime audit is clean and the available npm fix is a breaking downgrade.",
    "Serial Assistant still requires a real-UART Web loop for partial prompts, ANSI colors, bare LF/CRLF, RX/TX logging, keyboard controls, reconnect, same-port conflict, and simultaneous RTT operation. Automated and disconnected-device Web checks are complete but do not replace this gate.",
    "USB Web entry still needs macOS/Linux qualification. Standard NSIS and updater behavior still need a second clean Windows machine. Non-builtin flash algorithm paths have automated coverage but limited physical-target coverage."
  ],
  "next_actions": [
    "After the maintainer connects the UART fixture, restart the source GUI on the updated backend and qualify Serial Assistant RX/TX, ANSI, keyboard input, reconnect, explicit stop, same-port conflict, and concurrent RTT on separate ports.",
    "Reproduce the first-trigger V4 offline empty failure across cold starts and add device-output diagnostics if it recurs.",
    "Run loss-sensitive SystemView tests with a larger target RTT buffer and document the sustainable event rate.",
    "Qualify USB Web entry on current macOS and Linux systems.",
    "Qualify standard NSIS and older-client updater behavior on a clean Windows 10/11 machine."
  ],
  "continuation_protocol": [
    "Validate project memory and reconcile it with live Git and runtime state before acting.",
    "Preserve Serial Assistant implementation checkpoint 4e5ff35; continue with its pending real-UART qualification without repeating completed HPM, localization, SuperWatch, large-array symbol, SystemView, RTT terminal, automated Serial Assistant, or disconnected-device Web work.",
    "Follow the repository branch, automated gate, real-surface, and release-authority rules.",
    "Keep future memory updates consolidated; use Git history for completed chronology instead of appending session logs."
  ]
}
