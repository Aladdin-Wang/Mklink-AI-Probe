# 0.2.0 本地覆盖安装验收（2026-09-05）

## 范围

用户要求重建安装包、覆盖本机安装并整理交接。本轮收口现有在线/脱机安全操作、
全片擦除、型号联想、本地/U 盘 FLM 路径以及短步骤引导；保留既有能力限制。
版本信息仅作概括，长期芯片安全任务移到 [维护路线图](../ai/security-roadmap.md)。

仅构建标准 NSIS 本地候选，不发布、不改版本标签或更新索引。使用维护 builder
新增的 `--local-bundle`，不读取签名密钥，不生成 updater `.sig`；已有正式签名
流程保持不变。包仍无 Authenticode 签名，Windows 可能显示未知发布者。

## 验收结果

构建完成，**覆盖安装未完成**：以系统目录 PATH 启动 NSIS `/S /UPDATE`，
Windows UAC 返回“操作已被用户取消”，安装器未执行。原安装 EXE 哈希未改变。
不得将源码后端健康或旧安装运行状态记为新包验收通过。

- 打包、内置算法与在线/脱机定向测试：363 项通过。
- Python 全量：1845 项通过，12 项因 Windows 符号链接权限 `WinError 1314`
  无法建立测试前提而失败；未改变 ACL、权限或删改测试以掩盖限制。
- GUI 首轮版本文案断言过时，同步后全量 654 项通过（60 个文件）。
- 生产 Vue/TypeScript、PyInstaller sidecar、Tauri 与标准 NSIS 构建成功。
- sidecar 内 2225 个算法资产文件与本地源逐一比对一致；Skill ZIP 的运行时边界
  及资产校验通过，均含 7059 个目标、2224 个去重 FLM。
- 原始固件备份和唯一硬件证据保留，未因精简交接而删除。
- 本轮全量测试产生的带链接临时目录按规则保留；具体位置见本地测试日志，
  不强制删除。交接精简不等于清空 `.build`。

## 候选包

业务源码基线 `d30b884`；生产 GUI 资源快照提交 `7b7902e`，GUI 版本标识显示
业务源码基线。候选路径为 `.build/artifacts/v0.2.0-local-d30b884-20260905`。

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| Mklink-AI-Probe-v0.2.0-d30b884-x64-Setup.exe | 91584248 | `1598bf366abd092a3660f0c0f643fc1f18e33c9339cd611f2cd2960c7186fc39` |
| Mklink-AI-Probe-v0.2.0-d30b884-Skill.zip | 19369450 | `f9202c723c5b16415234942c87653327eef962050d06492a09aeacc3d0243dd3` |

清单与校验文件同目录保存，包不上传。安装前配置哈希和旧 EXE 哈希保留在本地
`.build/reports/local-install-20260905`；原硬件备份不变。

## 续接

用户准备确认 UAC 后，重新执行已保存的安装脚本，再检查：新 EXE 与构建哈希
一致、配置和快捷方式保留、干净 PATH 启动、自带 sidecar 健康、探针枚举、
进程树无 Python、实际界面和正常关闭后端口释放。当前源码后端仍在使用 8765，
启动安装版前须核对并停止这个已知服务，避免把旧服务响应当成新包结果。

正式发布需单独验收。此次打包不操作目标保护、擦写或 VCC，不替代真机报告。
