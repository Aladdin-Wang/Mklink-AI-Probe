# 0.2.0 正式发布验收

## 当前状态

2026-09-07：正式发布已获用户授权，当前准备阶段。尚未合并 master、创建版本标签或发布更新指针。完整 Python 测试的符号链接权限待解决，不以环境失败冒充通过。

## 本轮修改与验证

脱机配置在全片擦除选项旁持续说明：默认按扇区擦除，通常无需全片擦除；全片擦除显著增加烧录时间。勾选后提示变为警示色，取消后恢复。中英文一致；保持原有默认值、确认流程和实际擦除逻辑。

| 门禁 | 结果 |
| --- | --- |
| GUI 全量 | 64 个文件、682 项通过。 |
| 生产前端 | Vue 类型检查及 Vite 构建通过；仅既有大块体积提示。 |
| Chrome 实际界面 | 中英文、默认未勾选、勾选警示、取消恢复通过，无页面异常。只选择测试文件，不部署或操作芯片。 |
| Rust | 19 项通过。Windows 测试程序副本嵌入 Common-Controls v6 清单后运行；没有改动产品二进制或源代码。 |
| Python 全量 | 当前代码 1901 通过、12 项因 Windows 符号链接权限失败（WinError 1314），18 条警告。管理员启动被 Windows 返回取消，未执行提权测试；待补齐权限或明确豁免。 |
| 正式安装包 | 待完成测试门禁后从干净 master 构建和安装验收。 |
| 发布 | 待签名、七项资产校验及实际安装验收；更新指针最后发布。 |

用户报告 XMC4800 开启全片擦除导致执行显著延长。本轮增加使用提示，没有以 STM32 测试证明 XMC4800 的串口读取错误根因，也没有将该错误宣称修复。

## 既有证据与边界

完整真机范围和未关闭项见 [发布前 HIL](v0.2.0-prerelease-hil-20260907.md)，追加类型写入见 [SuperWatch 写入](v0.2.0-superwatch-write-20260907.md)。此前安装验收针对本地候选，不能替代本次正式包验收。高速 USB、RTT 启停残留等既有未关闭项继续保留。

本轮原始测试日志、浏览器截图、构建/签名/安装材料留在外层忽略目录 `.build/reports/release-0.2.0` 和 `.build/artifacts`；公共 Skill 不包含本报告或维护交接。

前端源码提交为 `30b6429`，生产界面构建标识固定为 `30b64295c0f1`。后续打包须使用 `VITE_APP_BUILD_COMMIT=30b64295c0f1` 和 `VITE_MKLINK_API=http://127.0.0.1:8765`，保持安装包与 Git 中 Skill 前端资源一致；发布清单另记录最终 master 源码提交。
