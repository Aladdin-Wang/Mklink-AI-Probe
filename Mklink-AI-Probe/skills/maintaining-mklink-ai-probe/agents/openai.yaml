interface:
  display_name: "Maintain Mklink AI Probe"
  short_description: "Shared cross-model repository maintenance workflow"
  default_prompt: "Use $maintaining-mklink-ai-probe to diagnose, implement, verify, document, and hand off this repository task consistently."

policy:
  allow_implicit_invocation: true
