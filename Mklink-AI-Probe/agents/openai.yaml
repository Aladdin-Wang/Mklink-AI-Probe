interface:
  display_name: "MKLink AI Probe"
  short_description: "Use MKLink for flashing, debugging, serial, and GUI tasks."
  default_prompt: "Use $mklink-ai-probe for this device task; read only the relevant operation reference."

policy:
  allow_implicit_invocation: true
